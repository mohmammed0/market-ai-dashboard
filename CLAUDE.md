# CLAUDE.md (Compatibility Stub)

This guide was moved to keep workflow metadata grouped outside the product-root reading path.

Canonical location:
- `docs/dev-workflows/CLAUDE.md`

Compatibility note:
- This root file is intentionally kept so existing tooling that expects `CLAUDE.md` at repo root keeps working.
